def calculate_expectancy(
    win_rate: float,
    average_win: float,
    average_loss: float
) -> float:
    """
    Returns the expected profit or loss per trade.

    win_rate should be between 0 and 1.
    average_win and average_loss should be positive values.
    """